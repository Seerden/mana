overwrite: true
schema: "http://localhost:5000/graphql"
# documents: ./backend/*.gql
documents: null
generates:
  ./frontend/src/graphql/codegen-output.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-operations"
      # - "typescript-urql"
    # config:
    #   withHooks: true
