overwrite: true
schema: "http://localhost:5000/graphql"
documents: ./backend/*.gql
generates:
  ./graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-operations"
      # - "typescript-urql"
    # config:
    #   withHooks: true
